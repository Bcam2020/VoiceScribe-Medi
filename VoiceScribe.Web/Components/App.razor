<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="VoiceScribe.Web.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
</head>
<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>

    <!-- Debug script for troubleshooting -->
    <script>
        console.log("Inline script running...");
        // Debug global access
        window.debugBlazor = {
            checkAudioRecorder: function() {
                console.log("Checking audioRecorder availability");
                if (typeof window.audioRecorder !== 'undefined') {
                    console.log("audioRecorder found!");
                    window.audioRecorderInitialized = true;
                    return true;
                } else {
                    console.log("audioRecorder NOT found!");
                    return false;
                }
            },
            forceInitialize: function() {
                console.log("Force initializing audioRecorder");
                window.audioRecorderInitialized = true;
                return true;
            }
        };
    </script>

    <!-- Add test.js before audioRecorder.js -->
    <script src="js/test.js"></script>

    <!-- Then add the audioRecorder.js file -->
    <script src="js/audioRecorder.js"></script>
</body>
</html>